<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.epfl.sweng.tutosaurus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeScreenActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MathsProfileActivity"
            android:label="Mathematics"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PhysicsProfileActivity"
            android:label="Physics"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ChemistryProfileActivity"
            android:label="Chemistry"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PublicProfileActivity"
            android:label="@string/title_activity_public_profile"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>